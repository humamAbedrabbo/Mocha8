@model AMS.Models.ClientType

@{
    ViewData["Title"] = Model.Name;
    ViewData["Action"] = "Details";

}

@inject AMS.Services.IUserService userService
<div class="row">
    <div class="col-md-3 col-sm-6">
        <div class="card">
            <div class="card-body p-0 d-flex align-items-center">
                <i class="icon-globe bg-info p-4 font-2xl mr-3"></i>
                <div>
                    <div class="text-value-sm text-info">@Model.Clients.Count()</div>
                    <div class="text-muted text-uppercase font-weight-bold small">Total Clients</div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-12">
        <div class="form-horizontal">
            <div class="card">
                <div class="card-header">
                    <strong>@ViewBag.Title</strong>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-12 mb-4">
                            <ul class="nav nav-tabs" role="tablist">
                                <li class="nav-item">
                                    <a class="nav-link active" data-toggle="tab" href="#general" role="tab" aria-controls="general">General</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" data-toggle="tab" href="#clients" role="tab" aria-controls="clients">Clients</a>
                                </li>
                            </ul>

                            <div class="tab-content">
                                <div class="tab-pane active" id="general" role="tabpanel">
                                    <div class="form-group row">
                                        <label class="col-2"><strong>@Html.DisplayNameFor(m => m.Name)</strong></label>
                                        <div class="col-10">@Model.Name </div>
                                    </div>
                                </div>
                                <div class="tab-pane" id="clients" role="tabpanel">
                                    <partial name="_Clients" for="@Model.Clients" />
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>

    </div>
</div>



@* view-scripts *@
@section Scripts {
    <script>
        $(function () {
            $(".table").bootstrapTable();
        });
    </script>
}